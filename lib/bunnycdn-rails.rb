# frozen_string_literal: true

# Bundler auto-require entrypoint: `gem "bunnycdn-rails"` runs `require "bunnycdn-rails"`,
# so the file name must match the gem name. Everything lives under Bunnycdn.
require_relative "bunnycdn"
